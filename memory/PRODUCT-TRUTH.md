# PRODUCT TRUTH — Drive Original

Evidence class: local source code plus dated automated/browser verification. External claims may be sourced only from entries that are `operational` or `verified`.

## Implemented

- `verified 2026-08-23`: Media list rendering keeps the complete population in memory while mounting no more than 240 cards. Evidence: `tests/app.test.js` G-drive-scale test (9,788 items) and desktop/mobile browser smoke.
- `historical 2026-08-23 behavior, superseded by D-038`: GIF cards did not attach `thumbnailLink` or any `<img>` and rendered only a static SVG placeholder. The current content-derived static-frame behavior is recorded below.
- `verified implementation 2026-08-23`: Move destinations exhaust the user corpus, enumerate every shared-drive root, supplement with per-drive folder scans at concurrency 2, retain orphan/cycle rows, and expose capability/API failures. Evidence: source review, pagination/catalog/capability tests, and current Google Drive API references.
- `verified 2026-08-23`: Shorts previous/next/random and random sort wait for the complete target-folder supported-media population and ignore display search/filter subsets. Evidence: population/order tests and browser interaction smoke.
- `historical through v1.19.0; superseded by v1.19.1`: The app requested only `https://www.googleapis.com/auth/drive`; the current explicit app-data scope contract is recorded below.
- `operational 2026-08-23`: GitHub Pages serves v1.14.0 at HTTP 200. Evidence: Actions runs 32646709841 and 32646842133 plus live `version.json`.
- `historical v1.14.1 behavior, superseded by D-037`: Favicon/PWA/Apple touch/maskable assets and the in-app header used a rounded navy logo with the historical blue dot removed. The restored canonical asset is recorded below.
- `operational 2026-09-16`: GitHub Pages serves v1.14.1 and the live `version.json` plus four PNG icon assets are byte-identical to Git blobs at release commit `9580da6254e9f2b73d15a3947e43a534d3ffc9d3`. Evidence: Actions run 35066797555, HTTP 200, and Node `Buffer.equals` readback.
- `observed 2026-09-16`: Windows desktop and Start menu shortcuts reference `C:\Users\jbs\AppData\Local\CustomAppIcons\drive_original_rounded_navy.ico`; the ICO contains 16, 24, 32, 48, 64, 128, and 256px entries and rendered successfully at 64px.
- `verified locally 2026-09-16`: v1.15.0 uses deliberate dominant-axis swipe thresholds with cancellation recovery, compact desktop playback controls, `,`/`.` and UI frame stepping, and a focus-contained responsive player. Evidence: source review, focused gesture tests, desktop/mobile demo interaction, and 35/35 Node tests.
- `verified locally 2026-09-16`: Long press on touch or right-click/Select on desktop enters multi-select; bulk trash and folder move use a four-worker task pool and retain only failed items after partial failure. Evidence: source review, deterministic task-pool/capability tests, and demo bulk-action flows.
- `historical v1.15.0 behavior, superseded by D-033 in v1.16.0`: The media proxy scoped tokens/errors to the requesting client, preserved Range and resource-key headers over one correlated 401 refresh retry, and aborted stale upstream work. That release deliberately disabled full-file video fallback and used a top-level Drive view; the current recovery policy is recorded below. Evidence: 10 service-worker contract tests plus app/static regression tests from the v1.15.0 release.
- `verified locally 2026-09-16`: Mobile demo navigation scores 100 for Lighthouse accessibility, best practices, SEO, and agentic browsing; an unthrottled local trace measured LCP 318ms and CLS 0.00. These are local demo measurements, not field data.
- `operational 2026-09-16`: GitHub Pages serves v1.15.0 from release commit `b813a15d29b97915b01b553dd758218e5451de86`. Actions run `35077604373` succeeded; live `version.json`, `app.js`, `styles.css`, `sw.js`, and `index.html` returned HTTP 200 and matched Git blobs byte-for-byte. Both 103,841-byte ZIPs have SHA-256 `8D87B29890DF0284CF95E003955D74E9E369BC906A96A298D39144EBED621059`; the Notion maintenance page was updated and re-fetched with matching release data.
- `verified locally 2026-09-16`: v1.16.0 keeps original-byte Range playback first; transport failures get one scoped retry and may then use writable OPFS or bounded memory for the same complete original. Codec/decode failures and download-restricted files use an in-app Google compatibility preview that is explicitly labeled as not guaranteeing original quality. Evidence: source review, service-worker recovery tests, buffer-policy tests, and responsive demo QA.
- `verified locally 2026-09-16`: v1.16.0 uses one real media decoder plus one reusable neighbour-poster layer. Horizontal order is frozen per session and extended only at its tail; vertical random playback waits for the complete target population, assigns two neighbours above and below, and spatially reverses to the prior item. Evidence: deterministic deck/order tests and 390×844 CDP touch traces showing 1:1 current/neighbor movement.
- `verified locally 2026-09-16`: Retry-After seconds and HTTP-date values are preserved, long server-requested waits do not trigger an early automatic retry, and Safari-class OPFS implementations without `FileSystemFileHandle.createWritable()` degrade to bounded memory rather than a broken disk path. Evidence: service-worker parsing tests and capability-gated source review.
- `operational 2026-09-16`: GitHub Pages serves v1.16.0 from release commit `6a669fba4441e53b478dc272989c49155ac6803b`. Actions run `35085992727` succeeded; live `version.json`, `app.js`, `styles.css`, `sw.js`, and `index.html` returned HTTP 200 and matched Git blobs byte-for-byte. Both 114,471-byte ZIPs have SHA-256 `2BB6D213BEB1C10B4517B527104FF69CED77E8C2F5E8E57BB08CA428DD47EC96`; the Notion maintenance page was updated, both ZIPs were attached, and the release data was re-fetched.
- `verified locally 2026-09-16`: v1.17.0 keeps every viable original-byte path ahead of Google compatibility playback. The worker verifies requested 206 ranges and `Content-Range`, reports Range-ignored 200 as sequential original transfer, preserves 416 size evidence, and returns structured session/status/reason/range/retry metadata. Authentication failures retry in-app and do not cause a quality downgrade. Evidence: source review, independent diff review, and 52/52 Node tests.
- `verified locally 2026-09-16`: Complete-original recovery streams to a temporary OPFS file when writable, otherwise uses strictly bounded memory; OPFS and memory share a three-request budget, validate complete 200 responses and byte counts, and clean fetches/readers/writers/object URLs/temporary files across cancellation and session replacement. Evidence: deterministic recovery, quota, limit, stale-session, and cleanup tests.
- `verified locally 2026-09-16`: Playback labels remain neutral until transport evidence exists, compatibility playback is explicitly marked as unverified original quality, demo content is explicitly non-original, and opening the player suspends pending background thumbnail work. Fresh desktop and 390×844 rehearsals had no console warnings/errors or horizontal overflow; mobile demo navigation measured LCP 414ms, CLS 0.00, and Lighthouse 100 for accessibility, best practices, SEO, and agentic browsing. These are local demo measurements, not field or authenticated-Drive evidence.
- `operational 2026-09-16`: GitHub Pages serves v1.17.0 from release commit `f53cde63d4da0524658a0633e10c325a02e566d9`. Actions run `35095971626` succeeded; live `version.json`, `app.js`, `styles.css`, `sw.js`, and `index.html` returned HTTP 200 and matched the release Git blobs byte-for-byte. Both 119,318-byte ZIPs contain the expected 16 entries, every file matches its Git blob, and have SHA-256 `1150740B3A5954F35C5788FCA5055D263ECF3CA8307C9D3A147F5913E8F0E19F`; the Notion maintenance page was updated, both ZIPs were attached, and the release data was re-fetched.
- `verified locally 2026-09-16`: The app ships the user-supplied web OAuth client ID as its default, starts sign-in only from the connection button, and retains an optional self-host override. The historical blue-dot PNG assets are restored byte-for-byte across regular, Apple touch, and maskable icon surfaces; the Windows custom shortcut ICO was backed up and replaced with the matching historical blue-dot ICO. Evidence: source/static contracts, historical Git/blob hashes, and local shortcut icon hash readback.
- `verified locally 2026-09-16`: Video startup adaptively chooses only between exact-original transports: known-size videos inside the storage guard may use OPFS first; large, unknown, unavailable, or exhausted-disk cases use Range. A failed OPFS route is recorded so Range failure cannot select it again; compatibility preview remains after original recovery and is labeled as unverified original quality. Evidence: deterministic route/exhaustion tests, original-recovery contracts, and 64/64 Node tests.
- `verified locally 2026-09-16`: GIF cards use a detached image only long enough to draw one static 320×320 cover frame. IntersectionObserver limits work to a 600px viewport margin, far cards release their canvas to 1×1, listing/session changes cancel active work, and player startup pauses the queue. Evidence: 7,384-file representative-folder inventory, an actual 1.78MiB GIF browser trial showing 320×320 near and 1×1 after exit, lifecycle tests, and zero browser console warnings/errors.
- `verified locally 2026-09-16`: Swipe targets freeze when an axis locks, commit uses that locked axis without a conflicting second dominance test, and the neighbour poster remains until the new decoder presents a frame. Mobile top and bottom overlays hide together on idle; an expanded action tray suppresses idle until it closes. Desktop uses one compact control rail with details/move/delete behind disclosure. Evidence: deterministic gesture/frame tests plus desktop and 390×844 browser readback with no horizontal overflow.
- `operational 2026-09-16`: GitHub Pages serves v1.18.0 from release commit `cf31107e290ad4b7540ed443691930f1c74a1f68`. Actions run `35107815036` succeeded; live `version.json`, `app.js`, `styles.css`, `sw.js`, and `index.html` returned HTTP 200 and matched the release Git blobs byte-for-byte. Both 101,876-byte ZIPs contain the expected 16 entries, every file matches its Git blob, and have SHA-256 `C26A82B9522F77BB8229B93D10BED69789260D978B917392246B09FEC4D845FD`; the Notion maintenance page was updated, both ZIPs were attached, and the release data was re-fetched.
- `verified 2026-09-16`: v1.18.1 accepts a CORS-hidden `Content-Range` only when the known Drive size and exposed positive safe-integer `Content-Length` exactly prove the requested bounded, open-ended, suffix, EOF-truncated, or HEAD interval. A visible mismatch, missing/unsafe evidence, short/long body, or out-of-bounds request fails closed; a Range-ignored 200 receives no synthetic Range support. Evidence: source review, independent Sol review, and 68/68 Node tests.
- `verified live 2026-09-16`: Authenticated Chrome loaded the real Drive folder and played a 207MB file and a 1.01GB file through the original Range path. The 1.01GB response reported `status=206`, `contentRangeInferred=true`, `rangeSatisfied=true`, reconstructed `bytes 0-1083524090/1083524091`, `readyState=4`, no media error, and playback beyond 13 seconds without an OPFS confirmation or Google compatibility downgrade. v1.18.1 emitted no new console warning/error. The earlier 978KiB sample also completed original OPFS playback.
- `operational 2026-09-16`: GitHub Pages serves v1.18.1 from release commit `cac0d0604d1d2d6fd30d863c6bc8c4e77143403b`. Actions run `35110287933` succeeded; five live core files returned HTTP 200 and matched release Git blobs byte-for-byte. Both 102,418-byte ZIPs contain 16 entries/15 files, every file matches its Git blob, and have SHA-256 `B5FF1DB7CC7BEF25C497DED75BEAE497EC1E246C6B898DA917BA906151B9DB53`; the Notion maintenance page was updated, both packages were attached, and targeted content was re-fetched.
- `verified locally 2026-09-17`: v1.19.0 stores file-ID-only viewed timestamps and favorite entries in one private Drive `appDataFolder` JSON file, caches them under the Drive account permission ID, merges viewed timestamps monotonically and favorite tombstones by their latest item timestamp, and schedules newer offline cache data back to Drive. No app server or database is added. Evidence: source review plus deterministic merge, multipart upload, reconnect/merge, and static privacy contracts.
- `verified locally 2026-09-17`: Random vertical shorts decks exhaust unseen account media before watched candidates while preserving random order inside each group. Mobile tap pairs toggle one favorite per pair in the media body, retain ±10-second seek only in the narrow video edges, and mobile left-edge library swipes commit exactly one back navigation after a dominant distance/velocity threshold. Evidence: deterministic gesture/deck tests.
- `verified locally 2026-09-17`: Cards, desktop player controls, and mobile shorts controls share the existing SVG/glass design language; the favorites filter collects liked images and videos across folders and removes an item immediately after unlike. Resetting a video before an image/GIF now recomputes control visibility so the stale central play button cannot remain. Evidence: desktop and 390×844 demo readback, cross-folder favorite/unfavorite interaction, zero browser warnings/errors, and the complete 78/78 Node suite.
- `operational 2026-09-17`: GitHub Pages serves v1.19.0 from release commit `51e5a28509aee062e71c1cac2774f5304ce44770`. Actions run `35116311314` succeeded; five live core files returned HTTP 200 and matched release Git blobs byte-for-byte. Both 110,591-byte ZIPs contain 16 entries/15 files, every file matches its Git blob, and have SHA-256 `7D07BA7679734B2D0D8E4C755FF81E9AF4471993FAD40EB007237FB5B44EA9E9`; the Notion maintenance page was updated, both packages were attached, and targeted content was re-fetched.
- `verified locally 2026-09-17`: v1.19.1 requests both `https://www.googleapis.com/auth/drive` and `https://www.googleapis.com/auth/drive.appdata`, versions the saved token contract so legacy drive-only tokens force one reconnect, and retries only transient account-state write failures with a bounded 1/2/4-second backoff. Evidence: source review, official appDataFolder scope documentation, token-migration/static contracts, and complete Node suite.
- `verified locally 2026-09-17`: The favorites projection reuses known local/catalog media and fetches only unresolved liked file IDs with bounded concurrency; it can retain partial results when state refresh or individual file access fails. Favorite requests are abortable and generation-scoped, while all library status writers use ownership tokens so a completed old request cannot restore text after a filter/folder change. Evidence: deterministic cache/fetch/failure/status tests and mobile demo favorite-to-all transition with an empty stale-status region.
- `verified locally 2026-09-17`: Mobile uses four equal text-only filter cells, a right-aligned vertical overflow stack 16px above `⋯`, and a transparent icon-only favorite feedback surface with a clipped accessible label. Desktop retains the existing player heart and balanced toolbar. Root breadcrumb construction now emits `내 드라이브` once even if stale duplicates are supplied. Evidence: 390×844 and 1280×800 DOM geometry/readback, icon-state screenshot, zero warning/error logs, breadcrumb regression test, and 84/84 Node tests passing three consecutive full runs.
- `operational 2026-09-17`: GitHub Pages serves v1.19.1 from release commit `c43b218ba3e74cbd692718660cd166d2419477e2`. Actions run `35121272050` succeeded; live `version.json`, `app.js`, `styles.css`, `sw.js`, and `index.html` returned HTTP 200 and matched release Git blobs byte-for-byte. Both 112,769-byte ZIPs contain 16 entries/15 files, every file matches its Git blob, and have SHA-256 `66CB642E2E432A54F9C88CD584CEEB19747D5C71C4137675E66A32C30190A9A8`; the Notion maintenance page was updated, both packages were attached, and targeted release content was re-fetched.

## Not implemented

- A live cross-drive move was not executed during verification because no authenticated, non-destructive test topology was available.
- Background Google token renewal after actual expiry and physical iOS Safari gestures were not exercised in this verification.
- Actual two-device propagation through a live Drive account was not exercised; local tests verify the Drive request/merge protocol, while physical iPhone double-tap and edge-swipe behavior remains device-specific evidence to collect.
- v1.19.1's expanded OAuth consent and direct favorite lookup were not exercised against the authenticated production account in this patch session; their request contracts and degraded local-cache behavior were verified deterministically.

## Permanently excluded

<!-- Link the decision ledger entry for every exclusion. -->
