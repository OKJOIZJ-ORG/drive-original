# Checkpoint — v1.17.0 original-quality hardening in progress — 2026-09-16 20:39

## The story so far

The user authorized the v1.17.0 original-quality-first hardening plan. Branch `codex/original-quality-first-hardening` contains committed decision/policy work (`6511ee4`) and Service Worker Range protocol hardening (`c974c6e`, 16/16 scoped tests). App state-machine, UI copy, README, and regression-test changes are present but still uncommitted while the playback worker finishes. Integration, independent review, browser QA, release packaging, Pages byte verification, and Notion readback remain.

## Decided

- D-035 keeps all in-app original-byte paths ahead of automatic Google compatibility playback; external Drive remains a manual escape hatch only.
- Service Worker responses now distinguish valid 206 Range, Range-ignored 200 sequential transfer, invalid Range responses, and 416 with structured recovery metadata.
- Physical iOS Safari and authenticated Google-account behavior remain explicit verification boundaries unless this release obtains fresh direct evidence.

## Waiting on the user

- None.

## Next first action

Wait for `/root/gpt_5_6_sol_high_app_playback_state_machine`, review its `app.js`/test diff, then run all Node checks before committing the integrated playback unit.

## Tried

- Gemini advisory consultation failed before content review because the local PowerShell runtime lacks `System.Convert.ToHexString`; do not retry this turn and rely on Codex plus an independent subagent review.
- The isolated Chrome DevTools profile has no saved OAuth client ID or Drive token, so authenticated real-account playback cannot be claimed from that profile.
