# Checkpoint — favorites and UI hardening in progress — 2026-09-17 01:10

## The story so far

The follow-up patch is active on `codex/mobile-action-stack`. It fixes the mobile action overflow as a vertical stack above `⋯`, makes the double-tap feedback an icon-only heart, and restores a balanced four-text filter row. The deeper favorites work now requests the required Drive `appDataFolder` OAuth scope, invalidates legacy tokens once, retries transient state sync failures, fetches liked files directly by ID instead of requiring a whole-Drive scan, keeps known local results during partial failure, aborts stale favorite loads, and gives each asynchronous library status update an ownership token so messages cannot linger after changing views. Four source files are modified; versioning, focused tests, full browser QA, records, commit, and release remain.

## Decided

- D-041 still governs account sync, unseen-first randomization, favorites, and mobile navigation.
- The user explicitly expanded this patch from the reported defects to a full PC/mobile UI and stability audit.
- The filter row uses four centered text labels (`전체`, `영상`, `이미지`, `좋아요`); the heart remains the state/action symbol in cards and players rather than duplicating it in the filter label.
- Private account state now requires both `drive` and `drive.appdata`; a one-time reconnect is intentional for legacy tokens.

## Waiting on the user

- None. Actual two-device propagation and physical iPhone Safari gestures remain device/account-specific evidence boundaries, not blockers for the patch.

## Next first action

Add discriminating regression tests for OAuth scope migration, favorite ID resolution, partial-failure fallback, and stale-status ownership, then run the full verification suite.

## Tried

- The earlier mobile action stack used `bottom: 62px`; live geometry showed overlap with `⋯`, so it was raised to `calc(var(--safe-bottom) + 84px)` and rechecked at 390×844.
- The in-app browser cannot inject physical iPhone touch hardware events; deterministic event tests remain required for double-tap and edge-swipe behavior.
- An initial automated click on the transient heart feedback missed its timing window; re-run with deterministic state inspection rather than treating that timeout as a product failure.
